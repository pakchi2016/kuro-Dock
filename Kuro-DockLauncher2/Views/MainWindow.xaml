<Window x:Class="Kuro_DockLauncher2.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KuroDock" SizeToContent="Height" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False"
        Loaded="Window_Loaded"
        >

    <Grid x:Name="DockPanel" HorizontalAlignment="Right" Background="#AA222222" Grid.Row="1" AllowDrop="True"
          MouseLeave="DockPanel_MouseLeave"
          ContextMenuOpening="DockPanel_ContextMenuOpening"
          ContextMenuClosing="DockPanel_ContextMenuClosing">
        <Grid.RenderTransform>
            <TranslateTransform X="99"/>
        </Grid.RenderTransform>

        <Grid.Style>
            <Style TargetType="Grid">
                <Style.Triggers>
                    <EventTrigger RoutedEvent="MouseLeftButtonDown">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                 To="0" Duration="0:0:0.2" >
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="DragEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             To="0" Duration="0:0:0.2" >
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>

        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="インデックスを追加" Click="AddIndex_Click"/>
                <Separator/>
                <MenuItem Header="終了" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="0"/>
            <ColumnDefinition Width="Auto" MinWidth="0"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <ItemsControl Grid.Column="0" ItemsSource="{Binding CurrentSubBookmarks}" 
                      Background="#99000000"
                      Visibility="Visible">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}" 
                            Width="150" Height="30" Margin="0,2,0,0"
                            Background="#55000000" Foreground="White"
                            BorderBrush="#77FFFFFF" BorderThickness="1"
                            Click="Bookmark_Click"
                            Opacity="0">
                        <Button.RenderTransform>
                            <TranslateTransform Y="-20"/>
                        </Button.RenderTransform>
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ItemsControl Grid.Column="1" ItemsSource="{Binding CurrentBookmarks}" 
                      Background="#77111111"
                      Visibility="Visible">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}" 
                            Width="150" Height="30" Margin="0,2,0,0"
                            Background="#55000000" Foreground="White"
                            BorderBrush="#77FFFFFF" BorderThickness="1"
                            Click="Bookmark_Click"
                            MouseEnter="Bookmark_MouseEnter" 
                            Opacity="0">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="名前を変更" Click="RenameBookmark_Click"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                        <Button.RenderTransform>
                            <TranslateTransform Y="-20"/>
                        </Button.RenderTransform>
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ItemsControl Grid.Column="2" ItemsSource="{Binding IndexItems}" Padding="0,10,0,100">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}" 
                            Width="90" Height="30" Margin="0,2,0,0" HorizontalAlignment="Right"
                            Background="#55000000" Foreground="White"
                            BorderBrush="#77FFFFFF" BorderThickness="1"
                            AllowDrop="True" Drop="IndexButton_Drop"
                            MouseEnter="IndexButton_MouseEnter"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Image Grid.Column="2" 
               Source="/Images/kuro.png"
               Width="96" Height="96"
               VerticalAlignment="Bottom" 
               HorizontalAlignment="Center" 
               Margin="0,0,0,3"
               IsHitTestVisible="False"/>
    </Grid>
</Window>
<Window x:Class="Kuro_Dock.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:Kuro_Dock"
    mc:Ignorable="d"
    Title="{Binding WindowTitle}" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="5">
            <ToolBar DockPanel.Dock="Top" Margin="0,0,0,5" DataContext="{Binding SelectedTab}">
                <Button Content="戻る" ToolTip="戻る" Command="{Binding NavigateBackCommand}"/>
                <Button Content="進む" ToolTip="進む" Command="{Binding NavigateForwardCommand}"/>
                <Button Content="↑" ToolTip="上へ" Command="{Binding GoUpCommand}"/>
                <Button Content="更新" ToolTip="最新の情報に更新" Command="{Binding RefreshCommand}"/>
            </ToolBar>
            <ToolBar DockPanel.Dock="Top" Margin="0,0,0,5">
                <Button Content="★+" ToolTip="現在の場所をブックマークに追加"
            Command="{Binding AddCurrentBookmarkCommand}"/>
                <Separator/>
                <ItemsControl ItemsSource="{Binding Bookmarks}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" Margin="2,0" ToolTip="{Binding Path}"
                        Command="{Binding DataContext.NavigateToBookmarkCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ToolBar>
            <TextBox Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center"
                     FontSize="14">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding NavigateToPathCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
        </DockPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="+" Command="{Binding AddNewTabCommand}" 
            VerticalAlignment="Top" Margin="5,2,0,0" Padding="5,2"/>

            <TabControl Grid.Column="1" ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Header}" VerticalAlignment="Center"/>
                        <Button Content="×" Margin="5,0,0,0" Padding="3,0" FontSize="10"
                                Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                CommandParameter="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" MinWidth="150"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*" MinWidth="200"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="0,0,1,0">
                            <TreeView ItemsSource="{Binding RootDirectories}">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <TextBlock Text="{Binding Name}" />
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Border>

                        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

                        <Border Grid.Column="2">
                            <ListView x:Name="ItemsListView" ItemsSource="{Binding DisplayedItems}" AlternationCount="2">
                                <b:Interaction.Triggers>
                                    <b:EventTrigger EventName="MouseDoubleClick">
                                        <b:InvokeCommandAction Command="{Binding OpenItemCommand}"
                                                               CommandParameter="{Binding ElementName=ItemsListView, Path=SelectedItem}"/>
                                    </b:EventTrigger>
                                </b:Interaction.Triggers>
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Width="30">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Image Source="{Binding Icon}" Width="16" Height="16"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="名前" DisplayMemberBinding="{Binding Name}" Width="250"/>
                                        <GridViewColumn Header="更新日時" DisplayMemberBinding="{Binding LastWriteTime, StringFormat='yyyy/MM/dd HH:mm'}" Width="150"/>
                                        <GridViewColumn Header="種類" DisplayMemberBinding="{Binding ItemType}" Width="120"/>
                                        <GridViewColumn Header="サイズ" DisplayMemberBinding="{Binding Size, StringFormat='N0'}" Width="100"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Border>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
        </Grid>

    </Grid>
</Window>